# Port du serveur (optionnel, défaut 3000)
PORT=3000

# Docker User ID Configuration (optionnel, défaut 1000)
# Définir ces valeurs pour correspondre à votre utilisateur hôte et éviter les problèmes de permissions
PUID=1000
PGID=1000
